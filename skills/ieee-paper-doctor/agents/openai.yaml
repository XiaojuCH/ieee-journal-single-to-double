interface:
  display_name: "IEEE Paper Doctor"
  short_description: "Audit and convert multi-file IEEEtran projects."
  default_prompt: "Use $ieee-paper-doctor to audit my full IEEEtran project, convert it into a safe two-column journal submission, and verify the PDF."

policy:
  allow_implicit_invocation: true
